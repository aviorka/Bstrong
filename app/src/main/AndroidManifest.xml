<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aviorka.bstrong">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen" android:label="ggg">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER"  />
            </intent-filter>
        </activity>
        <activity android:name=".AutoPicture" /> <!-- <activity android:name=".AutoPicture" /> -->
        <activity android:name=".Login" />
        <activity android:name=".JoinUs" />
        <activity android:name=".MyPlane" />
        <activity android:name=".EquipmentE" />
        <activity android:name=".fragment.ExercisePlan"></activity>
    </application>

</manifest>